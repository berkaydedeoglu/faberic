# faberic

## Project layout

```
agent/            agent services
  agents.yaml     agent registry/config
  agents/
    base/         CLI and HTTP API around the pi.dev coding agent SDK (Bun)
    discussion/   planned
    planner/      planned
orchestrator/      planned
artifact-hub/       planned
doc/                 planned
```

Currently, `agent/agents/base` is the only implemented service — see its
[README](agent/agents/base/readme.md) for details.
